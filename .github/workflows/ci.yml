name: Backend CI

on:
  push:
    branches:
      - main
      - 'agent/**'
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/bruna_affonso_ci
      JWT_SECRET: ci_jwt_secret_with_more_than_32_characters_123
      REFRESH_SECRET: ci_refresh_secret_with_more_than_32_chars_456
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Validate Prisma schema
        run: npx prisma validate

      - name: Check security-critical imports
        run: |
          node --check src/server.js
          node --check src/controllers/authController.js
          node --check src/services/refreshSessionService.js

      - name: Validate refresh migration files
        run: |
          test -f prisma/migrations/migration_lock.toml
          test -f prisma/migrations/20260827130000_refresh_sessions/migration.sql
          grep -q 'CREATE TABLE "RefreshSession"' prisma/migrations/20260827130000_refresh_sessions/migration.sql

      - name: Validate production start command
        run: |
          grep -qx 'web: npx prisma migrate deploy && node src/server.js' Procfile.txt
          test -f src/server.js
